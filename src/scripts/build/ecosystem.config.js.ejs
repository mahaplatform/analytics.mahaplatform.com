module.exports = {
  apps: [{
    name: 'analytics_cron_<%= environment %>',
    script: './cron.js',
    instances: 1,
    cwd: process.env.PWD+'/current',
    error_file: './logs/cron.err.log',
    out_file: './logs/cron.out.log',
    exec_mode: 'fork_mode',
    env: {
      NODE_ENV: '<%= environment %>'
    }
  },{
    name: 'analytics_worker_<%= environment %>',
    script: './worker.js',
    instances: 2,
    cwd: process.env.PWD+'/current',
    error_file: './logs/worker.err.log',
    out_file: './logs/worker.out.log',
    exec_mode: 'fork_mode',
    env: {
      NODE_ENV: '<%= environment %>'
    }
  }]
}
